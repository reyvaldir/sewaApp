version: "3.8"

services:
  web:
    # Mengambil image langsung dari Docker Hub, diparsing dari environment variable di VPS
    image: ${DOCKERHUB_USERNAME}/sewa-app:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
